describe('Argon2-PasswordAlgo With ARGON2ID and default values:', function () {

    it('\n1. Login should success when user registered by Argon2', function (done) {
    });

    it('\n2. login should success when update password from management api when Argon2 algo is set', function (done) {
    });

    it('\n3. Verify that login should success after Change Password when Argon2 algo is set', function (done) {
    });

    it('\n4. Verify that login should success after Reset Password when Argon2 algo is set', function (done) {
    });

    it('\n5. Verify that login should success after Reset Password by securityQuestionByEmail when Argon2 algo is set', function (done) {
    });

    it('\n6. Verify that login should success after Reset Password by securityQuestionByUserName when Argon2 algo is set', function (done) {
    });

    it("\n7. Verify that login should success after Reset Password by securityQuestionByUserId when Argon2 algo is set", function (done) {
    });

    it('\n8. login should success when SetPassword from management api when Argon2 algo is set', function (done) {
    });

    it('\n9. Verify onetouchlogin and email verification from api (oneTouchLogin)', function (done) {

    });

});

describe('Argon2-PasswordAlgo With ARGON2ID and Base64 value:', function () {

    it('\n1. Login should success when user registered by Argon2', function (done) {
       
    });

    it('\n2. login should success when update password from management api when Argon2 algo is set', function (done) {
       
    });

    it('\n3. Verify that login should success after Change Password when Argon2 algo is set', function (done) {
        
    });

    it('\n4. Verify that login should success after Reset Password when Argon2 algo is set', function (done) {
        
    });

    it('\n5. Verify that login should success after Reset Password by securityQuestionByEmail when Argon2 algo is set', function (done) {

    });

    it('\n6. Verify that login should success after Reset Password by securityQuestionByUserName when Argon2 algo is set', function (done) {
        
    });

    it("\n7. Verify that login should success after Reset Password by securityQuestionByUserId when Argon2 algo is set", function (done) {
        
    });

    it('\n8. login should success when SetPassword from management api when Argon2 algo is set', function (done) {
        
    });

    it('\n9. Verify onetouchlogin and email verification from api (oneTouchLogin)', function (done) {
       
    });


});

describe('Argon2-PasswordAlgo With ARGON2ID and UTF8 value:', function () {

    it('\n1. Login should success when user registered by Argon2', function (done) {
    });

    it('\n2. login should success when update password from management api when Argon2 algo is set', function (done) {
    });

    it('\n3. Verify that login should success after Change Password when Argon2 algo is set', function (done) {
    });

    it('\n4. Verify that login should success after Reset Password when Argon2 algo is set', function (done) {
    });

    it('\n5. Verify that login should success after Reset Password by securityQuestionByEmail when Argon2 algo is set', function (done) {
    });

    it('\n6. Verify that login should success after Reset Password by securityQuestionByUserName when Argon2 algo is set', function (done) {
    });

    it("\n7. Verify that login should success after Reset Password by securityQuestionByUserId when Argon2 algo is set", function (done) {
    });

    it('\n8. login should success when SetPassword from management api when Argon2 algo is set', function (done) {
    });

    it('\n9. Verify onetouchlogin and email verification from api (oneTouchLogin)', function (done) {

    });


});

describe('Argon2-PasswordAlgo With ARGON2I and default values:', function () {

    it('\n1. Login should success when user registered by Argon2', function (done) {
    });

    it('\n2. login should success when update password from management api when Argon2 algo is set', function (done) {
    });

    it('\n3. Verify that login should success after Change Password when Argon2 algo is set', function (done) {
    });

    it('\n4. Verify that login should success after Reset Password when Argon2 algo is set', function (done) {
    });

    it('\n5. Verify that login should success after Reset Password by securityQuestionByEmail when Argon2 algo is set', function (done) {
    });

    it('\n6. Verify that login should success after Reset Password by securityQuestionByUserName when Argon2 algo is set', function (done) {
    });

    it("\n7. Verify that login should success after Reset Password by securityQuestionByUserId when Argon2 algo is set", function (done) {
    });

    it('\n8. login should success when SetPassword from management api when Argon2 algo is set', function (done) {
    });

    it('\n9. Verify onetouchlogin and email verification from api (oneTouchLogin)', function (done) {
    });

});

describe('Argon2-PasswordAlgo With ARGON2I and Base64 value:', function () {

    it('\n1. Login should success when user registered by Argon2', function (done) {
    });

    it('\n2. login should success when update password from management api when Argon2 algo is set', function (done) {
    });

    it('\n3. Verify that login should success after Change Password when Argon2 algo is set', function (done) {
    });

    it('\n4. Verify that login should success after Reset Password when Argon2 algo is set', function (done) {
    });

    it('\n5. Verify that login should success after Reset Password by securityQuestionByEmail when Argon2 algo is set', function (done) {
    });

    it('\n6. Verify that login should success after Reset Password by securityQuestionByUserName when Argon2 algo is set', function (done) {
    });

    it("\n7. Verify that login should success after Reset Password by securityQuestionByUserId when Argon2 algo is set", function (done) {
    });

    it('\n8. login should success when SetPassword from management api when Argon2 algo is set', function (done) {
    });

    it('\n9. Verify onetouchlogin and email verification from api (oneTouchLogin)', function (done) {
    });

});

describe('Argon2-PasswordAlgo With ARGON2I and UTF8 value:', function () {

    it('\n1. Login should success when user registered by Argon2', function (done) {
    });

    it('\n2. login should success when update password from management api when Argon2 algo is set', function (done) {
    });

    it('\n3. Verify that login should success after Change Password when Argon2 algo is set', function (done) {
    });

    it('\n4. Verify that login should success after Reset Password when Argon2 algo is set', function (done) {
    });

    it('\n5. Verify that login should success after Reset Password by securityQuestionByEmail when Argon2 algo is set', function (done) {
    });

    it('\n6. Verify that login should success after Reset Password by securityQuestionByUserName when Argon2 algo is set', function (done) {
    });

    it("\n7. Verify that login should success after Reset Password by securityQuestionByUserId when Argon2 algo is set", function (done) {
    });

    it('\n8. login should success when SetPassword from management api when Argon2 algo is set', function (done) {
    });

    it('\n9. Verify onetouchlogin and email verification from api (oneTouchLogin)', function (done) {
    });

});

describe('Argon2-PasswordAlgo With Customer Algo:', function () {
    this.timeout(constants.TEST_TIMEOUT_60000_MS);

    it('\n1. Login should success when user registered by Argon2', function (done) {
    });

    it('\n2. login should success when update password from management api when Argon2 algo is set', function (done) {
    });

    it('\n3. Verify that login should success after Change Password when Argon2 algo is set', function (done) {
    });

    it('\n4. Verify that login should success after Reset Password when Argon2 algo is set', function (done) {
    });

    it('\n5. Verify that login should success after Reset Password by securityQuestionByEmail when Argon2 algo is set', function (done) {

    });

    it('\n6. Verify that login should success after Reset Password by securityQuestionByUserName when Argon2 algo is set', function (done) {
    });

    it("\n7. Verify that login should success after Reset Password by securityQuestionByUserId when Argon2 algo is set", function (done) {
    });

    it('\n8. login should success when SetPassword from management api when Argon2 algo is set', function (done) {
    });

    it('\n9. Verify onetouchlogin and email verification from api (oneTouchLogin)', function (done) {
    });

});