describe('HMAC_SHA1-PasswordAlgo: @S3cb5e47c', function () {

    it('\n1. Login should success when user registered by HMAC_SHA1 @Tecdf50ae', function (done) {
    });

    it('\n2. login should success when update password from management api when HMAC_SHA1 algo is set @Tbb07a286', function (done) {
    });

    it('\n3. Verify that login should success after Change Password when HMAC_SHA1 algo is set @Tecfd41ca', function (done) {

    });

    it('\n4. Verify that login should success after Reset Password when HMAC_SHA1 algo is set @T305383fc', function (done) {
    });

    it('\n5. Verify that login should success after Reset Password by securityQuestionByEmail when HMAC_SHA1 algo is set @T1c6cf98b', function (done) {
    });

    it('\n6. Verify that login should success after Reset Password by securityQuestionByUserName when HMAC_SHA1 algo is set @T4fb4ca42', function (done) {
    });

    it("\n7. Verify that login should success after Reset Password by securityQuestionByUserId when HMAC_SHA1 algo is set @T310eed2d", function (done) {
    });

    it('\n8. login should success when SetPassword from management api when HMAC_SHA1 algo is set @T678a8900', function (done) {
    });

});

// HMAC-SHA1 with SaltAttachType - Append, IsPerPasswordSalt - True, PasswordHashEncodingType : Base64, Salt blank
describe('HMAC_SHA1-PasswordAlgo:', function () {

    it('\n1. Login should success when user registered by HMAC_SHA1 @Tecdf50ae', function (done) {
    });

    it('\n2. login should success when update password from management api when HMAC_SHA1 algo is set @Tbb07a286', function (done) {
    });

    it('\n3. Verify that login should success after Change Password when HMAC_SHA1 algo is set @Tecfd41ca', function (done) {
    });

    it('\n4. Verify that login should success after Reset Password when HMAC_SHA1 algo is set @T305383fc', function (done) {
    });

    it('\n5. Verify that login should success after Reset Password by securityQuestionByEmail when HMAC_SHA1 algo is set @T1c6cf98b', function (done) {
    });

    it('\n6. Verify that login should success after Reset Password by securityQuestionByUserName when HMAC_SHA1 algo is set @T4fb4ca42', function (done) {
    });

    it("\n7. Verify that login should success after Reset Password by securityQuestionByUserId when HMAC_SHA1 algo is set @T310eed2d", function (done) {
    });

    it('\n8. login should success when SetPassword from management api when HMAC_SHA1 algo is set @T678a8900', function (done) {
    });

});

// HMAC-SHA1 with SaltAttachType - Prepend, IsPerPasswordSalt - True, PasswordHashEncodingType : Base64, Salt blank
describe('HMAC_SHA1-PasswordAlgo:', function () {

    it('\n1. Login should success when user registered by HMAC_SHA1', function (done) {
    });

    it('\n2. login should success when update password from management api when HMAC_SHA1 algo is set', function (done) {
    });

    it('\n3. Verify that login should success after Change Password when HMAC_SHA1 algo is set', function (done) {
    });

    it('\n4. Verify that login should success after Reset Password when HMAC_SHA1 algo is set', function (done) {
    });

    it('\n5. Verify that login should success after Reset Password by securityQuestionByEmail when HMAC_SHA1 algo is set', function (done) {
    });

    it('\n6. Verify that login should success after Reset Password by securityQuestionByUserName when HMAC_SHA1 algo is set', function (done) {
    });

    it("\n7. Verify that login should success after Reset Password by securityQuestionByUserId when HMAC_SHA1 algo is set", function (done) {
    });

    it('\n8. login should success when SetPassword from management api when HMAC_SHA1 algo is set', function (done) {
    });

});
