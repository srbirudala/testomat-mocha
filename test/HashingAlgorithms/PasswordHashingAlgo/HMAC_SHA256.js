describe('HMAC_SHA256Config-PasswordAlgo: @Sb822502e', function () {


    it('\n1. Login should success when user registered by HMAC_SHA256Config @Te0ad2ce3', function (done) {
    });

    it('\n2. login should success when update password from management api when HMAC_SHA256Config algo is set @T02b130c4', function (done) {
    });

    it('\n3. Verify that login should success after Change Password when HMAC_SHA256Config algo is set @Tbe8d2d65', function (done) {
    });

    it('\n4. Verify that login should success after Reset Password when HMAC_SHA256Config algo is set @T999217d2', function (done) {
    });

    it('\n5. Verify that login should success after Reset Password by securityQuestionByEmail when HMAC_SHA256Config algo is set @T9a16c8a4', function (done) {
    });

    it('\n6. Verify that login should success after Reset Password by securityQuestionByUserName when HMAC_SHA256Config algo is set @Tb45a5076', function (done) {
    });

    it("\n7. Verify that login should success after Reset Password by securityQuestionByUserId when HMAC_SHA256Config algo is set @T277c343b", function (done) {
    });

    it('\n8. login should success when SetPassword from management api when HMAC_SHA256Config algo is set @T204b20aa', function (done) {
    });

});


// HMAC_SHA256Config with SaltAttachType - Append, IsPerPasswordSalt - True, PasswordHashEncodingType : Base64, Salt blank
describe('HMAC_SHA256Config-PasswordAlgo:', function () {

    it('\n1. Login should success when user registered by HMAC_SHA256Config @Te0ad2ce3', function (done) {
    });

    it('\n2. login should success when update password from management api when HMAC_SHA256Config algo is set @T02b130c4', function (done) {
    });

    it('\n3. Verify that login should success after Change Password when HMAC_SHA256Config algo is set @Tbe8d2d65', function (done) {
    });

    it('\n4. Verify that login should success after Reset Password when HMAC_SHA256Config algo is set @T999217d2', function (done) {
    });

    it('\n5. Verify that login should success after Reset Password by securityQuestionByEmail when HMAC_SHA256Config algo is set @T9a16c8a4', function (done) {
    });

    it('\n6. Verify that login should success after Reset Password by securityQuestionByUserName when HMAC_SHA256Config algo is set @Tb45a5076', function (done) {
    });

    it("\n7. Verify that login should success after Reset Password by securityQuestionByUserId when HMAC_SHA256Config algo is set @T277c343b", function (done) {
    });

    it('\n8. login should success when SetPassword from management api when HMAC_SHA256Config algo is set @T204b20aa', function (done) {
    });

});


// HMAC_SHA256Config with SaltAttachType - Prepend, IsPerPasswordSalt - True, PasswordHashEncodingType : Base64, Salt blank
describe('HMAC_SHA256Config-PasswordAlgo:', function () {

    it('\n1. Login should success when user registered by HMAC_SHA256Config', function (done) {
    });

    it('\n2. login should success when update password from management api when HMAC_SHA256Config algo is set', function (done) {
    });

    it('\n3. Verify that login should success after Change Password when HMAC_SHA256Config algo is set', function (done) {
    });

    it('\n4. Verify that login should success after Reset Password when HMAC_SHA256Config algo is set', function (done) {

    });

    it('\n5. Verify that login should success after Reset Password by securityQuestionByEmail when HMAC_SHA256Config algo is set', function (done) {
    });

    it('\n6. Verify that login should success after Reset Password by securityQuestionByUserName when HMAC_SHA256Config algo is set', function (done) {
    });

    it("\n7. Verify that login should success after Reset Password by securityQuestionByUserId when HMAC_SHA256Config algo is set", function (done) {
    });

    it('\n8. login should success when SetPassword from management api when HMAC_SHA256Config algo is set', function (done) {
    });

});