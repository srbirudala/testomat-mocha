describe('HMAC_SHA256_BitEncryptedConfig-PasswordAlgo:', function () {

    it('\n1. Login should success when user registered by HMAC_SHA256_BitEncryptedConfig', function (done) {
    });

    it('\n2. login should success when update password from management api when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

    it('\n3. Verify that login should success after Change Password when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

    it('\n4. Verify that login should success after Reset Password when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

    it('\n5. Verify that login should success after Reset Password by securityQuestionByEmail when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

    it('\n6. Verify that login should success after Reset Password by securityQuestionByUserName when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

    it("\n7. Verify that login should success after Reset Password by securityQuestionByUserId when HMAC_SHA256_BitEncryptedConfig algo is set", function (done) {
    });

    it('\n8. login should success when SetPassword from management api when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

});


// HMAC_SHA256_BitEncryptedConfig with SaltAttachType - Append, IsPerPasswordSalt - True, PasswordHashEncodingType : Base64, Salt blank
describe('HMAC_SHA256_BitEncryptedConfig-PasswordAlgo:', function () {

    it('\n1. Login should success when user registered by HMAC_SHA256_BitEncryptedConfig', function (done) {
    });

    it('\n2. login should success when update password from management api when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

    it('\n3. Verify that login should success after Change Password when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

    it('\n4. Verify that login should success after Reset Password when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

    it('\n5. Verify that login should success after Reset Password by securityQuestionByEmail when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

    it('\n6. Verify that login should success after Reset Password by securityQuestionByUserName when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

    it("\n7. Verify that login should success after Reset Password by securityQuestionByUserId when HMAC_SHA256_BitEncryptedConfig algo is set", function (done) {
    });

    it('\n8. login should success when SetPassword from management api when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

});


// HMAC_SHA256_BitEncryptedConfig with SaltAttachType - Prepend, IsPerPasswordSalt - True, PasswordHashEncodingType : Base64, Salt blank
describe('HMAC_SHA256_BitEncryptedConfig-PasswordAlgo:', function () {

    it('\n1. Login should success when user registered by HMAC_SHA256_BitEncryptedConfig', function (done) {
    });

    it('\n2. login should success when update password from management api when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

    it('\n3. Verify that login should success after Change Password when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

    it('\n4. Verify that login should success after Reset Password when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

    it('\n5. Verify that login should success after Reset Password by securityQuestionByEmail when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

    it('\n6. Verify that login should success after Reset Password by securityQuestionByUserName when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

    it("\n7. Verify that login should success after Reset Password by securityQuestionByUserId when HMAC_SHA256_BitEncryptedConfig algo is set", function (done) {
    });

    it('\n8. login should success when SetPassword from management api when HMAC_SHA256_BitEncryptedConfig algo is set', function (done) {
    });

});