describe('HMAC_SHA256ConfigPhone-PasswordAlgo: Phone, Email and OneTouchLogin @Se8d0a6d8', function () {

    it('\n9. Verify Reset password from Email OTP and successfully login when HMAC_SHA256ConfigPhone algo is set @T596302a2', function (done) {
    });

    it('\n10. Verify that one_touch_login_by_email api should support HMAC_SHA256ConfigPhone algo @Td54a551b', function (done) {
    });

    it('\n11. Verify PUT One Touch Login Verification, POST OneTouchLogin By Phone APIs @Te7fbc40a', function (done) {
    });

});


describe('HMAC_SHA256ConfigPhone-PasswordAlgo: Phone, Email and OneTouchLogin', function () {

    it('\n9. Verify Reset password from Email OTP and successfully login when HMAC_SHA256ConfigPhone algo is set @T596302a2', function (done) {
    });

    it('\n10. Verify that one_touch_login_by_email api should support HMAC_SHA256ConfigPhone algo @Td54a551b', function (done) {
    });

    it('\n11. Verify PUT One Touch Login Verification, POST OneTouchLogin By Phone APIs', function (done) {
    });

});

describe('HMAC_SHA256ConfigPhone-PasswordAlgo: Phone, Email and OneTouchLogin', function () {

    it('\n9. Verify Reset password from Email OTP and successfully login when HMAC_SHA256ConfigPhone algo is set', function (done) {
    });

    it('\n10. Verify that one_touch_login_by_email api should support HMAC_SHA256ConfigPhone algo', function (done) {
    });

    it('\n11. Verify PUT One Touch Login Verification, POST OneTouchLogin By Phone APIs', function (done) {
    });

});